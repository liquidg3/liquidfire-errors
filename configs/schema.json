{
    "properties": {
        "prefix": {
            "type": "string",
            "options": {
                "label": "Prefix",
                "description": "Prefixed to all logs"
            }
        }
    }
}